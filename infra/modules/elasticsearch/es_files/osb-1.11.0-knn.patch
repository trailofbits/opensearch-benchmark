diff --git osbenchmark/workload/params.py osbenchmark/workload/params.py
index 9ba99f71..6ee3077a 100644
--- osbenchmark/workload/params.py
+++ osbenchmark/workload/params.py
@@ -1183,7 +1183,8 @@ class VectorSearchPartitionParamSource(VectorDataSetPartitionParamSource):
             A dictionary containing the body used for search query
         """
         query = {
-            "vector": vector,
+            "field": self.field_name,
+            "query_vector": vector,
             "k": self.k,
         }
         if efficient_filter:
@@ -1192,9 +1193,7 @@ class VectorSearchPartitionParamSource(VectorDataSetPartitionParamSource):
             })
 
         knn_search_query = {
-            "knn": {
-                self.field_name: query,
-            },
+            "knn": query,
         }
 
         if self.is_nested:
